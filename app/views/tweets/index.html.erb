<h1>Tweets</h1>

<div class="max-w-sm rounded overflow-hidden shadow-lg">
    <div class="px-6 pt-4 pb-2">
    <% if Current.user.twitter_accounts.any? %>
        <%= link_to "Schedule a Tweet", new_tweet_path, class: 'bg-green-500 hover:bg-blue-700 text-white font-bold py-2 px-4 border border-blue-700 rounded' %>
    <% end %>
    </div>
</div>

<div class="max-w-sm rounded overflow-hidden shadow-lg">
    <div class="px-6 pt-4 pb-2">
    <% if Current.user.twitter_accounts.none? %>
        <%= link_to "Connect Your Twitter Account", "/auth/twitter", class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 border border-blue-700 rounded' %>
    <% end %>
    </div>
</div>